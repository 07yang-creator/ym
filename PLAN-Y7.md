# Y7 — 角色编码体系 · 志愿者任务系统 · 嘉宾报名

Owner brief + 追加答复, 2026-07-22. 与 `ym/PLAN.md` §E / §E.5b 一起读。
**状态（2026-07-27 收盘）：Y7 全部建成并上线。** 0015 + 0016 已应用；SMTP 已配（Resend，
验证域 `send.jjconnect.tokyo`，发件人 `no-reply@send.jjconnect.tokyo`）。套件 328 项绿。
之后追加并上线的：用户一览 tab · 复盘区分本人申报/主办登记 · 嘉宾报名闭环 · 加人时直接发码 ·
**全 app 线条图标**（owner universal rule）· 自助改密码/忘记密码。
**进行中**：票据四处入口 + 收支总账（设计工作流跑完后再建，见 §J）。

---

## A. 角色 = 编码 = 用户身份（这条改动最大）

「这些角色既是本 APP 的用户角色定义。」定案见 A2 —— 凭证与编号分开，有偿服务者的 N 放末尾。

### A1. 这替换掉上周的授权码机制，不是叠加

| | 上周做的（授权码） | Y7（邀请码） |
|---|---|---|
| 码是什么 | GoTrue 的 6 位 `email_otp` | **我们自己的** 16 位随机码 → 烧掉后留下编号 `V0014` |
| 谁先有邮箱 | **主办必须先知道 TA 的邮箱**才能生成 | 主办只发码，**邮箱由用户注册时自己填** |
| 码的寿命 | 24 小时 | **一周** |
| 绑定方式 | 靠邮箱字符串匹配 | **靠码本身** |
| 登录 | 邮箱+密码 | **码 或 邮箱** + 密码 |

**好处不只是短**：主办不再需要预先收集邮箱，`/api/ym_invite`（持 service key 的那个高危端点）
和它的 GoTrue link 生成可以整个退休 —— 攻击面直接少一块。

### A2. 编码规则（owner 定案 2026-07-22）

**凭证和编号分开** —— 这是这套设计的关键，两者要求相反：

```
邀请码（凭证 · 用一次就烧 · 一周有效）   7K2M9XQ4B8T3W6H      16 位随机

用户编码（永久 · 显示用）—— 位数即角色识别特征，所以补零显示：
  H001      主办者       3 位顺位   ← 仅平台管理员可发
  V0014     志愿者       4 位顺位
  V0014N    有偿服务者   4 位顺位（与 V 同一号池，N 只表示拿钱）
  D0007     捐赠者       4 位顺位
  O003      记者 / 旁观  3 位顺位
  P482719   嘉宾         6 位【随机】← 不排序
```

- **凭证 16 位随机** → 不可枚举，所以限速是纵深防御而不是唯一防线。字符集剔除 `0/O/1/I/l`
  等易混字形；输入自动大写、忽略空格与连字符。
- **位数 = 角色的视觉签名**，所以补零（`V0014` 而非 `V14`）。输入仍然容错：`v 14`、`V014`
  都按数字归一到同一人，但显示与存储一律是补零的规范形。
- **嘉宾用 6 位随机、不用顺位** —— 顺位会泄露「这家一共多少嘉宾」「这人大概何时来的」，
  对相亲活动的参加者是真实隐私。随机没有这个问题。生成时查重重试（10⁶ 空间，
  按每年千人算，碰撞概率可忽略）。
- **其余角色用顺位**，在数据库里原子取号（唯一约束 + 冲突重试），两人同时加不会撞号。
- **`V…N` 与 `V` 共用一个号池** —— 否则 `V0014` 和 `V0014N` 会是两个不同的人，必然被误会。
- ⚠ **顺位号可枚举**（V0001、V0002…），攻击者能列出全部志愿者账号去撞密码。这不是漏洞
  （邮箱同样可枚举），但**登录端点必须按账号限速**。嘉宾因为随机，天然没有这个面。
- 容量：H/O 999 · V/D 9,999 · P 999,999。满了再扩位，**旧编号不受影响**（角色字母决定位数）。

### A3. 注册与登录（owner 简化定案）
- **注册**：邀请码 + 自己的邮箱 + 自设密码 → 完成。码即刻烧掉，编号永久落在 TA 的记录上。
- **登录：只用邮箱 + 密码。**
- **不做「用编码登录」** —— owner 拍板简化。这一刀砍掉整个码登录端点：不需要服务端做
  编码→邮箱 解析，就没有邮箱收割 oracle；也不必为「顺位号可枚举」做按账号限速。
  **少一个端点、少一类攻击面。**
- 编码因此是**纯显示身份**（名册卡、成员页、服务经历上的那个号），不参与任何认证路径。
- **忘记密码走标准邮件流程**（SMTP 配好即可），主办不用介入 —— 见 §H。
### A4. 安全红线（0014 §(d) 的延续）
角色写在码里，**码决定你成为谁**：
- **H 码只有平台管理员能生成** —— 生成一个 H 码 = 造一个主办方 = 可向任意地址发布内容。
  主办自己只能发 V/V…N/P/D/O。**服务端强制**，不是隐藏按钮。
- **兑换时角色由码决定**，前端传上来的 role 一律忽略。
- 兑换失败仍然限速（按 IP + 按码）—— 16 位已经不可枚举，这一层是防撞库和防刷。

### A5. 一个界面，六种角色
「除主办者以外，其他角色共用同一个界面，显示与其角色和任务相关的内容。」
`ym/member/index.html` 从「志愿者页/活动页」升级为**统一成员页**，按 `role` 决定区块：

| 区块 | V 志愿者 | P 嘉宾 | D 捐赠 | V…N 有偿 | O 旁观 |
|---|:-:|:-:|:-:|:-:|:-:|
| 公开活动信息 | ● | ● | ● | ● | ● |
| 当天流程（时间+环节名） | ●（含说明） | ●（无说明） | – | ●（含说明） | ● |
| 我要做的事 / 交任务 | ● | – | – | ● | – |
| 可领任务（发布池） | ● | – | – | – | – |
| 我的号牌 / 我想参加 | – | ● | – | – | – |
| 贡献 · 工时 · 自费 · 星级 | ● | – | – | ● | – |
| 主办方通知 | ● | ● | ● | ● | ● |
> D/VN/O 的具体内容你还没细说，我先按上表给最小集合，做出来你再改。
> **嘉宾资料仍然只有主办可见** —— 这条不因为角色体系而松动。

---

## B. 志愿者任务系统

### B1. 难度（点选）
`chip.diff`，默认 `C`，可选 `C/B/A/S/SS`。**计划和执行两处都能点选**（你确认了要点选式）。

### B2. 交任务
志愿者在自己那条任务上填 **工时 / 自费 / 简短说明** → 「交任务」。
- `0015` 给 `ym_submit` 加 `hours` `cost` `diff`，`kind` 加 `'submit'` `'claim'` `'join'`。
- **自费不进收支**（你写的「无需主办方承担」）—— 是贡献记录，绝不参与活动差额。

### B3. 交付 / 退回（你已确认）
- 交任务 → 卡片**立刻左移，显示「交付」**（虚线，区别于实线的「完成」）。
- **交付是投影，不是状态写入**：`chip.status` 仍然只有主办点确认才变。
- 主办打开 → 看到工时/自费/说明/票据 → **确认完成**（→完成）或 **退回**（→右侧「未完成」+必填说明）。
- 退回后志愿者那边显示 **「该任务未被确认，需要您跟进」**，点开看主办的反馈，跟进后**再次提交**。

### B4. 贡献
成员页底部折叠：完成件数 · 累计工时 · 累计自费 · 主办的感谢 · 服务经历。
数据来自已确认的 `ym_submit` + 复盘的 `volLog`。**工时不重复计算**：志愿者自报的进累计，
复盘里主办填的单独显示为「主办登记」。

### B5. 星级会限制接任务（你确认）
`p.rank`（C/B/A/S/SS，主办授予）。**任务难度 > 本人星级 → 不能接**。
界面上**显示但锁住**（`🔒 需 S 级`），不隐藏 —— 藏起来只会让人以为没任务，锁住才有升级动力。

### B6. 任务发布 + 抢单（你确认）
- 大部分任务主办直接指定；**未指定的，主办一键发布**（`chip.open=true`）。
- 志愿者**没有公共看板**，发布的任务出现在**各自的个人页面**上。
- **先点先得**：第一个点「接任务」的拿到，后点的收到 **「该任务已经被领取」**。
- **抢单必须在服务端定胜负**，不能靠前端先到先写：做成一个原子 RPC
  `ym_claim_task(host, event_id, chip_id)`，用 `insert … on conflict do nothing` 判定，
  返回 `ok` / `taken` / `rank-too-low`。两个人同时点，数据库只会让一个成功。
- **hub-and-spoke**：任务池只带 环节/时间/难度/说明，**绝不带其他志愿者的名字**；被领走后
  其他人只看到「已被领取」，不知道是谁。

---

## C. 嘉宾报名

- 嘉宾登录后看到**公开活动列表**（`e.publicOn` 的那些），点开某场 → 「我想参加」。
- → `ym_submit` 一行 `kind:'join'`，主办在 计划→参加者 看到，同意后 `fileGuest()` + 发号牌。
- **通知：先只做 app 内提示**（你说邮件迟点配，主办暂时手动发）。app 内提示位置：
  成员页顶部一条，以及号牌出现即视为通过。

---

## D. 建每一步都能单独上线、单独验证

| # | 步骤 | 风险 |
|---|---|---|
| 1 | `0015`：角色编码表 + 兑换 RPC + 限速；`ym_submit` 加 hours/cost/diff/新 kind | 中 |
| 2 | 主办侧：名册加人时一键发码（V/P/D/VN/O），码即编号显示 | 低 |
| 3 | 成员页：改成「码/邮箱 + 密码」注册登录；按 role 分区块 | **高 — 换掉登录** |
| 4 | 难度点选（计划+执行）+ 星级授予 | 低 |
| 5 | 交任务（工时/自费/说明）+ 交付投影 + 退回跟进 | **中 — 动执行树** |
| 6 | 一键发布 + 抢单 RPC + 星级门槛 | **高 — 新的共享形状** |
| 7 | 嘉宾 我想参加 + 主办同意 | 中 |

第 3、6 步各自单独做一次对抗式复查 —— 前两次严重问题都出在「谁能看到/写到什么」变宽的地方。

---

## E. 我替你定的（可推翻）

1. 自费不进收支；2. 交付是投影；3. 工时不重复计；4. 难度默认 C；
5. 星级不够的任务**显示并锁住**，不隐藏；6. H 码仅管理员可生成，主办只能发 V / V…N / P / D / O；
7. 抢单由数据库定胜负，不由前端；8. 兑换失败仍限速（16 位已不可枚举，这层是防撞库/防刷）；
9. 顺位在数据库原子取号，不在前端算。

---

## F. 开工前

**F1. ~~关掉 Confirm email~~ —— 已在代码里解决，你不用动后台。**
本来是个死结：Supabase 自带邮件发不到团队外地址，确认信永远收不到 → 注册了也是死号。
但注册本来就要经过我们自己的端点（要在服务端校验邀请码），所以直接用 service key
`POST /auth/v1/admin/users {email, password, email_confirm:true}` 建号 —— 不发确认信，
建出来就是已确认。后台设置保持原样，以后配了 SMTP 也不冲突。

**F2. 确认现有的 4 个志愿者/嘉宾账号怎么处理。**
如果 ljzhujudy 已经用上周的授权码给谁开过号，那些人是用邮箱绑定的。
新体系按码绑定 —— 我会让旧的邮箱绑定继续有效（不动 `ym_claim_shares`），
但需要你确认**有没有已经在用的账号**，有的话我给他们补发编号而不是让他们重注册。

---

## G. 拆除清单（旧授权码路径 —— owner 已确认没有存量账号，不做兼容层）

**删**
- `api/ym_invite.py` 整个退休（持 service key 的 GoTrue link 生成端点）+ `vercel.json` 里的注册。
- organizer：`loadShareStates` / `SHARE_ST` / `grantState` / `grantPaint` / `grantIssue` /
  `grantSend`（L749–853 一带）—— 全部是「先有邮箱才能发 OTP」那套。
- member：`useCode`（`verifyOtp`）、`needPw` / `setPw`（`must_set_password` 那条 UX 标记）。
- `scripts/check-ym.mjs` 中断言旧路径的项（L273、L292、L613、L662–664、L706+）。

**留**
- **`0012_ym_invite.sql` 不动** —— 那是主办方注册用的，等于新体系的 H 角色，仍然只有管理员能发。
  新的 H 码只是把它的**码格式**变短（`YM-XXXX-XXXX-XXXX` → 16 位凭证 + `H001`），
  兑换后仍然置 `ym_member.status='approved'`。V/P/D/O 的兑换**绝不碰 `ym_member`**。
- `grantRevoke`（取消授权仍然要有，只是改成按码解绑）。
- `pushShares` / `shareDataFor` / `guestDataFor` / `dropShare`（改绑定键，逻辑保留）。
- `ym_submit` 全套（0014）+ 交任务/确认/退回机制。

**名册记录要新增**：`p.code`（16 位凭证，烧掉后清空）、`p.uid`（顺位或嘉宾随机号）、
`p.role`（H/V/P/D/O）、`p.paid`（V…N 的 N）、`p.codeExp`（一周）。

---

## H. SMTP（owner 决定配置，2026-07-22）

Email confirmation 已由 owner 在后台关闭。配 SMTP 的目的是**找回密码**（以及将来想开确认信）。
选 **Resend 免费版**：3,000 封/月 · 100 封/日 · 1 个域名 —— 沙龙量级绰绰有余。

1. resend.com 注册（免费版）
2. 加域名，**用子域 `mail.jjconnect.tokyo`**（官方建议：隔离发信声誉，不连累主域）
3. Cloudflare 加 Resend 给的 SPF(TXT+MX) 与 DKIM(TXT)。
   ⚠ **CNAME 的云朵必须是灰色（DNS only）** —— 橙色代理会让验证失败，Cloudflare 用户最常踩的坑。
4. Resend → API Keys → Create。**这个 key 就是 SMTP 密码。**
5. Supabase → Authentication → SMTP Settings：
   `smtp.resend.com` · 端口 `587`（或 465/SSL）· 用户名字面量 `resend` · 密码 = API Key ·
   发件人 `no-reply@mail.jjconnect.tokyo`
6. ⚠ **启用自定义 SMTP 后 Supabase 自动压到 30 封/小时** —— 去 Rate Limits 调高，
   否则一场活动同时发几十封会**静默失败**。
7. 验证：member 页点一次「忘记密码」，确认真的收到。

---

## I. 上线后剩下的（2026-07-27）

**已拍板**
- ✅ **有偿服务者（V…N）能抢单 —— 保持现状**（owner, 2026-07-27）。§A5 那张表当初只给了 V，
  以表为准是错的：发布层和网关层本来就一致。**纯供应商以后另开一个 C 类**，现在不做。
- **D / VN / O 三个角色的成员页内容**是按 §A5 最小集合做的，你看过实际效果再调。

**待观测（静态检查替代不了）**
- **抢单的并发单赢家性质**是靠 Postgres 推测插入语义论证的，不是观测的。真正的验证是
  两个人（或两台手机）**同时点同一个任务** —— 一个拿到、另一个看到「该任务已经被领取」。
- **邮件链路**：member 页点一次「忘记密码」，确认真能收到。

**已知的、故意如此的**
- 部署后主办需**保存一次**池子才有内容；在那之前抢单一律回答无效（失败方向安全，但比较安静）。
- 志愿者赢了要等主办点**确认指派**才真正归他 —— 只有主办会话能写台本，没有更快的路径。
- 「已被领取」是**会话内**的，刷新后那张卡看起来又可点。把它写进 payload 等于把
  「有人动过这里」推送给所有人，违反 hub-and-spoke。

---

## J. 票据 + 总账（2026-07-27 提出，设计中）

**owner 要求**
1. 票据入口四处：任务（主办和志愿者都能传）· 主办台（随时录入）· 复盘（补录）· 捐助者（提交）
2. 票据按钮沿用 jjcashflow 的形式：拍照 → 读领収書 → 抓总额 + 判类别 + 记票据号
3. 主办要一份收入/开支总账，「满足一般会计规范即可」

**已查清的现状**
- **识别那套已经有了**：`api/parse.py` 默认 mode 就返回 `total_incl_tax` / `suggested_account`
  (勘定科目) / `invoice_reg_no` (T+13) / `tax_lines`(按 10・8・0・非課税 分行) / `direction`。
  缺的**不是识别，是入口和落账**。
- 现在只有两处：志愿者在任务上传、主办的相机圆钮走「票据 → 收支」。
  **主办在任务上传、复盘补录、捐助者提交这三处都没有。**
- **捐助者有个具体障碍**：D 角色的 payload 是 `kind:'info'`，不带任何 `chipId`，而写入权限正是靠
  「你能证明这个 chip 发布给过你」判定 —— 所以他们现在一个字节都写不进来。要让他们交票据，
  必须单独开一条最小写入路径。
- 钱现在只以「预算牌」形式活在每场活动里（`e.header.money[]` / `e.rows[].money[]`），
  **没有跨活动的账**。总账要么是对现有数据的投影，要么需要新的存储。

**红线**（设计必须守住）
- `ym_doc.payload` 是主办独有且**没有大小 CHECK**（0015 §4 标记的唯一无上限面），
  800ms 防抖同步 —— **base64 票据图绝不能写进去**。
- 成员（志愿者/捐赠者）写票据一律走 `ym_submit`，**confirm-first**，主办转换。
- **自费不进总账**：那是贡献记录，不是账目。
- AI 辅助、人确认：解析结果是草稿，主办一键确认，绝不自动入账。
